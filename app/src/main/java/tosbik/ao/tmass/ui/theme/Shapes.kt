package tosbik.ao.tmass.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(4.dp),  // Use for buttons, chips, etc.
    medium = RoundedCornerShape(8.dp), // Use for cards, dialogs, etc.
    large = RoundedCornerShape(16.dp)  // Use for larger surfaces, like modals
)